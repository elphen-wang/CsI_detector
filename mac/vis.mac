# Use this open statement to create an OpenGL view:
/vis/open OGL

# Draw geometry:
/vis/drawVolume

# Specify view angle:
/vis/viewer/set/viewpointThetaPhi 45 45

# Zoom up/down:
/vis/viewer/zoom 1.5

# Draw coordinate axes:
/vis/scene/add/axes 0 0 0 10 cm


# 1. 创建一个基于粒子 ID 的绘图模型
/vis/modeling/trajectories/create/drawByParticleID

# 2. 设置电子 (e-) 为红色 (red)
/vis/modeling/trajectories/drawByParticleID-0/set e- red

# 3. 设置正电子 (e+) 为蓝色 (blue)
/vis/modeling/trajectories/drawByParticleID-0/set e+ blue

# (可选) 设置伽马射线 (gamma) 为绿色
/vis/modeling/trajectories/drawByParticleID-0/set gamma green

# 设置光子 (opticalphoton) 为青色
/vis/modeling/trajectories/drawByParticleID-0/set opticalphoton cyan

# ==============================================================
# 过滤光子显示 (如果光子太多导致卡顿或看不清，请取消注释以下命令)
# ==============================================================
# 创建粒子过滤器
# /vis/filtering/trajectories/create/particleFilter
# 添加光子到过滤器
# /vis/filtering/trajectories/particleFilter-0/add opticalphoton
# 反转过滤器 (true 表示隐藏列表中的粒子，false 表示只显示列表中的粒子)
# /vis/filtering/trajectories/particleFilter-0/invert true
# ==============================================================

# ==============================================================
# 只显示那些曾进入 CsI_crystal 体积的整条轨迹（使用 encounteredVolumeFilter）
# 注意：此过滤器会保留整条轨迹（包括在 world 中的段），但只保留那些
#       曾进入指定体积的轨迹。如果你需要只绘制晶体内部的段，需使用
#       drawByEncounteredVolume 模型（部分 Geant4 构建可能不包含该模型）。
# ==============================================================
/vis/filtering/trajectories/create/encounteredVolumeFilter
/vis/filtering/trajectories/encounteredVolumeFilter-0/add CsI_crystal
/vis/filtering/trajectories/encounteredVolumeFilter-0/invert false
# ==============================================================

# Draw smooth trajectories at end of event, showing trajectory points
# as markers 2 pixels wide:
/vis/scene/add/trajectories smooth
/vis/modeling/trajectories/create/drawByCharge
/vis/modeling/trajectories/drawByCharge-0/default/setDrawStepPts true
/vis/modeling/trajectories/drawByCharge-0/default/setStepPtsSize 2

# To superimpose all of the events from a given run:
/vis/ogl/set/displayListLimit 10000
/vis/scene/endOfEventAction accumulate 1000
